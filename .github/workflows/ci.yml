name: Nexora CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: xcode-27
    steps:
      - uses: actions/checkout@v5
      - name: Test
        run: swift test
      - name: Release build
        run: swift build -c release
